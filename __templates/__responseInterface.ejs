<% data.forEach(d => { %>

<% d.types.forEach(type => { %>
/**
 * <%- d.description %>
 */
type <%- type.name %> = <%- type.itemName %>[];
<% }); %>

<% d.interfaces.forEach(interface => { %>
/**
 * <%- d.description %>
 */
interface <%- interface.name %> {
  <% interface.properties.forEach(prop => { %>
  <%- prop.name %>: <%- prop.type %>
  <% }); %>
}
<% }); %>

<% }); %>
